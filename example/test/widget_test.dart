// Widget tests require the native ailia_tracker library, which is only
// bundled when running as a full application. The tracker behavior is
// verified with the integration test in integration_test/.
void main() {}
